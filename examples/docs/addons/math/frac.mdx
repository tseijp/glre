---
title: 'frac'
description: 'Extract fractional components to create infinite tiling patterns, crystalline structures, and mathematical harmony in visual space'
keywords: [glre, frac, fractional, modulo, tiling, crystalline, infinite, mathematical, patterns]
date: 2025-08-06
---

import { FragmentEditor } from '@site/src/theme/FragmentEditor'

# Fractional Universe Generator

### Infinite Patterns Through Mathematical Precision

The fractional function extracts the decimal portion of numbers, transforming continuous mathematical space into repeating, crystalline structures. This fundamental operation enables the creation of infinite tiling patterns, geometric repetitions, and mathematical harmonies that extend beyond the boundaries of conventional space.

By capturing only the fractional remainder, complex coordinate systems collapse into elegant, repeating units that form the foundation for procedural textures, architectural patterns, and natural crystalline formations.

<FragmentEditor
        isFun
        code={`
const fragment = () => {
        const tiled = frac(uv.mul(8.0))
        const pattern = smoothstep(0.1, 0.2, tiled.x).mul(smoothstep(0.1, 0.2, tiled.y))
        const gradient = length(tiled.sub(0.5)).mul(2)
        return vec4(vec3(pattern.mul(gradient)), 1)
}`}
/>

### Crystalline Frequency Modulation

Fractional decomposition creates rhythmic pulsations when combined with temporal elements. The mathematical precision of fractional extraction generates perfectly synchronized, crystalline frequency patterns that evolve through time with mathematical inevitability.

<FragmentEditor
        isFun
        code={`
const fragment = () => {
        const timeScale = iTime.mul(0.3)
        const coord = uv.add(timeScale).mul(12)
        const fracX = frac(coord.x)
        const fracY = frac(coord.y)
        const interference = sin(fracX.mul(TWO_PI).add(timeScale.mul(4))).mul(sin(fracY.mul(TWO_PI).add(timeScale.mul(3))))
        const intensity = interference.add(1).div(2)
        return vec4(intensity.mul(0.9), intensity.mul(0.7), intensity.mul(1.2), 1)
}`}
/>

### Dimensional Fractal Architecture

The fractional function enables construction of self-similar architectural structures that exist across multiple dimensional scales. Each fractional layer creates geometric recursion, building infinite complexity from simple mathematical principles.

<FragmentEditor
        isFun
        code={`
const fragment = () => {
        const pos = uv.sub(0.5).mul(6)
        const layer1 = frac(pos.mul(1))
        const layer2 = frac(pos.mul(3))
        const layer3 = frac(pos.mul(9))

        const structure = layer1.x.mul(layer1.y).add(
                layer2.x.mul(layer2.y).mul(0.5)
        ).add(
                layer3.x.mul(layer3.y).mul(0.25)
        )

        const depth = abs(sin(structure.mul(PI).add(iTime.mul(2))))
        const architectural = smoothstep(0.2, 0.8, depth)

        return vec4(architectural.mul(1.3), architectural.mul(0.8), architectural.mul(0.4), 1)

}`}
/>

### Quantum Grid Materialization

Fractional space division creates quantum-like grid systems where matter appears to materialize at precise mathematical coordinates. The fractional boundaries become portals where visual elements phase in and out of existence with mathematical predictability.

<FragmentEditor
        isFun
        code={`
const fragment = () => {
        const zoom = sin(iTime.mul(0.4)).mul(3).add(6)
        const grid = frac(uv.mul(zoom))
        const centerDistance = length(grid.sub(0.5)).mul(2)

        const quantum = step(0.8, centerDistance).sub(step(0.95, centerDistance))
        const phaseShift = sin(grid.x.mul(PI).mul(8).add(iTime.mul(6))).mul(
                sin(grid.y.mul(PI).mul(8).add(iTime.mul(4)))
        ).add(1).div(2)

        const materialization = quantum.mul(phaseShift)
        const energy = pow(materialization, 2).mul(2)

        return vec4(energy.mul(0.3), energy.mul(1.2), energy.mul(0.9), 1)

}`}
/>
