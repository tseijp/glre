---
title: 'torus'
description: 'Generate parametric torus geometry with configurable major radius, minor radius, and segment density for topological surface analysis'
keywords: [torus, parametric surface, topology, revolution surface, donut geometry]
date: 2025-12-27
---

# Torus

import { FragmentEditor } from '@site/src/theme/FragmentEditor'

<FragmentEditor
        isApp
        code={`
function Canvas() {
        const geo = torus({ radius: 0.5, tube: 0.25, radialSegments: 8, tubularSegments: 32 })
        const mat = rotate3dX(iDrag.y).mul(rotate3dY(iDrag.x))
        const gl = useGL({
                isWebGL: true,
                isDepth: true,
                count: geo.count,
                vertex: vec4(mat.mul(geo.vertex), 1),
                fragment: vec4(varying(geo.normal), 1),
        })
        return <canvas ref={gl.ref} />
}
`}
/>

## Torus Props

| **radius**          | Major radius from torus center to tube center.<br />Default is `1`.     |
| ------------------- | ----------------------------------------------------------------------- |
| **tube**            | Minor radius of the tube cross-section.<br />Default is `0.4`.          |
| **radialSegments**  | Number of segments around the tube circumference.<br />Default is `12`. |
| **tubularSegments** | Number of segments along the torus circumference.<br />Default is `48`. |
| **arc**             | Central angle sweep in radians.<br />Default is `Math.PI*2`.            |

### Parametric Surface Definition

The torus represents a surface of revolution generated by rotating a circle around a coaxial axis. The mathematical definition employs two angular parameters forming a coordinate system on the surface.

$$
\begin{aligned}
x(u,v) &= (R + r\cos v) \cos u \\
y(u,v) &= (R + r\cos v) \sin u \\
z(u,v) &= r \sin v
\end{aligned}
$$

Where $u \in [0, 2\pi]$ parameterizes the major circumference and $v \in [0, 2\pi]$ parameterizes the minor circumference. The major radius $R$ controls the distance from the central axis to the tube center, while the minor radius $r$ determines the tube thickness.

### Surface Normal Calculation

The normal vectors are computed by taking the cross product of the partial derivatives with respect to both parameters. For a torus, the normal at any point $(u,v)$ points radially outward from the nearest point on the central axis.

$$
\vec{n}(u,v) = \frac{\partial \vec{r}}{\partial u} \times \frac{\partial \vec{r}}{\partial v}
$$

The resulting normal vector simplifies to:

$$
\vec{n}(u,v) = (\cos v \cos u, \cos v \sin u, \sin v)
$$

### Topological Properties

The torus exhibits genus-1 topology, containing exactly one handle. This fundamental property distinguishes it from spherical surfaces (genus-0) and creates non-trivial parametric relationships across the surface.

The ratio $\frac{r}{R}$ determines the torus type. When $r < R$, a standard torus forms. As $r$ approaches $R$, the inner hole shrinks. When $r > R$, the surface develops a self-intersection, creating a spindle torus configuration.
