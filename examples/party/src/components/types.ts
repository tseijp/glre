export type Props = {
        isHUD?: boolean
        isMenu?: boolean
        isModal?: boolean
        hasProfile?: boolean
        Colors?: any[]
        culturalWorld?: any
        page?: string
        children?: any
        onSignIn?: () => void
}
